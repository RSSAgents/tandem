# Дата: 2025-02-19

- **Что было сделано:**

Исправление базовых настроек ESLint, Prettier, Husky + RTL setup

- **Проблемы:**

Файлы с настройками в корне проекта с монорепозиторием, не работающий husky.
Установка на такой проект RTL вело к разбросу записей в devDependencies(package.json записывал во вложенную папку frontend, package-lock.json записывало в корневую папку).

- **Решения (или попытки):**

Удаление файлов из корня проекта, удаление зависимостей RTL.
Установка и настройка eslint, prettier, husky внутри основной папки frontend.
Установка RTL внутри папки frontend.
Удаление избыточных зависимостей, таких как eslint-plugin-perfectionist, eslint-plugin-import, eslint-plugin-unused-imports.
Расширил настройки для eslint-config.

- **Мысли / Планы:** Что буду делать дальше?

Оформление дневника в ветку main
Авторизация к vercel через github
Фикс роутинга

- **Затраченное время:**

5 часов
